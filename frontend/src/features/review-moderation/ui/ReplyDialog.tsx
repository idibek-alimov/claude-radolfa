"use client";

interface ReplyDialogProps {
  reviewId: number;
  existingReply: string | null;
}

/** Stub — replaced with full implementation in Phase 4. */
export function ReplyDialog(_props: ReplyDialogProps) {
  return null;
}
